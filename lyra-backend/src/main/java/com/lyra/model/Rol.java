package com.lyra.model;

/**
 * Enum que define los roles disponibles para un usuario dentro de la aplicación
 * Se utiliza para controlar permisos y funcionalidades (ADMIN o USER)
 */


public enum Rol {
    ADMIN,
    USER
}
